{
  "name": "lumina",
  "version": "0.4.0",
  "description": "Manga/Manhwa/Manhua Translation Automation Desktop App",
  "author": "Kiznavierr",
  "type": "module",
  "main": "dist/main/main.js",
  "scripts": {
    "build": "npm run build:main && npm run build:preload && npm run build:renderer && npm run typecheck",
    "build:main": "esbuild src/main/main.ts --bundle --platform=node --format=esm --external:electron --external:electron-updater --outfile=dist/main/main.js",
    "build:preload": "esbuild src/preload.ts --bundle --platform=node --format=cjs --external:electron --outfile=dist/preload/preload.cjs",
    "build:renderer": "esbuild src/renderer/renderer.ts --bundle --outfile=dist/renderer/bundle.js --format=iife --target=es2022 --loader:.html=text && node scripts/copy-renderer-assets.mjs",
    "typecheck": "tsc -p tsconfig.json",
    "start": "npm run build && electron dist/main/main.js",
    "dev": "npm run build && electron dist/main/main.js",
    "bundle:dml": "node scripts/bundle.mjs --variant=dml",
    "bundle:cuda": "node scripts/bundle.mjs --variant=cuda",
    "dist:dml": "npm run build && npm run bundle:dml && electron-builder --config scripts/electron-builder.config.mjs --publish never",
    "dist:cuda": "npm run build && npm run bundle:cuda && electron-builder --config scripts/electron-builder.config.cuda.mjs --publish never",
    "dist:all": "npm run build && npm run bundle:dml && npm run bundle:cuda && electron-builder --config scripts/electron-builder.config.mjs --publish never && electron-builder --config scripts/electron-builder.config.cuda.mjs --publish never"
  },
  "dependencies": {
    "electron-updater": "^6.8.9",
    "konva": "^10.3.1",
    "lucide": "^1.33.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "electron": "^43.4.1",
    "electron-builder": "^26.15.3",
    "esbuild": "^0.28.2",
    "typescript": "^5.9.3"
  }
}
